const cores = ['verde', 'vermelho', 'azul']
console.log(cores[2])

cores[3] = 'amarelo'
console.log(cores)
console.log(cores.length) // retorna o tamanho da array
cores.push('laranja') // Add na Array
delete cores[1]